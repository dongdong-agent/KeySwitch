# KeySwitch（Rust 版）接班文档

> 记录时间：2026-08-18
> 交接人：Hermes Agent（小羽）
> 项目位置：`L:\00-projects\apikey-switcher-rust`（Rust/Tauri 2 重构版）
> 旧版（Python，仍可用）：`L:\00-projects\apikey-switcher`

---

## 一、任务背景

东哥要把 KeySwitch（API Key 管理器，原 Python/tkinter 版）**重构为 Rust 版本**，放到独立目录。
功能对齐 Python 版 v3.1（每软件独立选 key + 智能切换 + 优先级 + 自助管理），UI 参考 OneWork
（左侧深色导航 + 右侧操作区，紫主题 #7c3aed）。

**技术选型**：Tauri 2 + React 18 + TypeScript + Vite（与 OneWork 同栈，托盘用官方 tray-icon 插件）。

## 二、当前状态总览（重要！）

| 项目 | 状态 |
|---|---|
| Rust 后端全部模块（models/adapters/usage/smart/commands/lib） | ✅ 编译通过（cargo check/build 无错误，仅 unused 警告） |
| React 前端（6 页面 + 导航 + 托盘逻辑） | ✅ tsc + vite build 通过 |
| **首次打包** | ✅ 成功：`src-tauri\target\release\bundle\nsis\KeySwitch_0.3.1_x64-setup.exe`（NSIS + MSI） |
| **首次运行验证**（15:05 左右的旧二进制） | ✅ 窗口正常、UI 完整、选项卡切换正常、用量/配置数据正确 |
| **配置迁移**（Python yaml → TOML） | ✅ 已执行，真实 key 已入 `%APPDATA%\KeySwitch\config.toml` |
| **opencode-go key 切换**（用户要求 pi+Hermes 换新 key） | ✅ 已完成：pi auth.json / Hermes 环境变量 / KeySwitch 池（Python+Rust 两处）全部换成 `sk-***`，实测 minimax-m3 与 deepseek-v4-flash 均 200 |
| **智能切换三维度判定**（滚动/周/月任一达阈值） | ✅ 代码已改并编译通过（usage.rs is_exhausted 取三维度最大值） |
| ❌ **最新二进制启动后窗口不显示** | ⚠️ **未解决，见第五节** |

## 三、已完成的功能清单（代码层面）

1. **左侧导航 + 右侧操作区**（OneWork 紫主题）：总览 / Key 配置 / Provider / Key 池 / 应用 / 设置
2. **每软件独立映射**：软件 × Provider 表格，每格独立下拉选 Key；「保存并应用」只写变化的项（先备份）
3. **智能切换**：检测在用 key，**滚动/周/月 任一**达阈值（默认 100%，可调）→ 切到该 provider 优先级最高可用 key，所有用它的软件一起切；后台定时（默认 5 分钟）或「立即检测并切换」手动触发；切换记录 + 需重启软件提示
4. **优先级排序**：Key 池页 ↑↓ 按钮调整（keys 列表顺序即优先级）
5. **自助管理**：界面添加/删除 Provider / API Key / 应用；8 种适配器（pi / env_var / openchatcut / workbuddy / codex / file_json / file_env / file_regex）
6. **系统托盘**：左键单击/菜单打开主窗口，菜单显示用量快照 + 智能切换状态；关闭窗口 = 隐藏到托盘（不退出）
7. **用量检测**：opencode-go `/usage`（percent 型）、DeepSeek `/user/balance`（balance 型）

## 四、技术架构与关键文件

```
apikey-switcher-rust/
├── src/                        # React 前端
│   ├── App.tsx                 # 左侧导航 + 页面切换
│   ├── pages.tsx               # 6 个页面组件 + Modal + 智能切换卡片
│   ├── lib/api.ts              # tauri invoke 封装 + TS 类型（JS camelCase → Rust snake_case 自动转换）
│   └── App.css                 # OneWork 紫主题
├── src-tauri/
│   ├── src/
│   │   ├── lib.rs              # 入口：托盘（TrayIconBuilder）+ 窗口隐藏（on_window_event CloseRequested）
│   │   ├── models.rs           # Config/Provider/KeyItem/Target/AutoSwitch + TOML 读写
│   │   ├── adapters.rs         # 8 种适配器（write_key/read_key/restart_hint）
│   │   ├── usage.rs            # HTTP 用量查询 + is_exhausted（三维度判定）
│   │   ├── smart.rs            # smart_switch_once（按优先级）+ move_key
│   │   └── commands.rs         # 全部 #[tauri::command]（配置/用量/应用/增删/智能切换）
│   ├── Cargo.toml              # tauri 2 + tray-icon + serde/toml/dirs/regex/ureq
│   └── tauri.conf.json         # 窗口 1120x680、identifier com.dongdong.keyswitch
└── tools/
    ├── migrate_config.py       # Python yaml → TOML 迁移（已执行）
    └── switch_opencode_key.py  # 一键切换 pi + Hermes + 池 的 opencode-go key（已执行）
```

**配置位置**：`%APPDATA%\KeySwitch\config.toml`（TOML 格式，可手改，界面改自动写回）

**关键设计决策**：
- 配置用 TOML 而非 YAML（Rust 生态标准，`toml` crate）
- 智能切换相关函数**不自动写盘**（Python 版踩过"测试配置覆盖真实配置"的坑，改为调用方显式 save）
- 适配器参数声明式配置（新增软件 90% 只改 config.toml 不用写代码）

## 五、⚠️ 未解决问题（接手第一优先）

### 5.1 Rust 版窗口不显示（严重）

**现象**：最新编译的 `KeySwitch.exe`（15:22）启动后进程在（~29MB 内存），但：
- `FindWindowW("KeySwitch - API Key 管理")` 返回 0（无窗口）
- computer_use / PowerShell MainWindowTitle 都找不到窗口
- 后来进程也自行退出了

**之前的旧二进制（15:05 首包）运行正常**（窗口显示、UI 验证通过）。
差异：15:22 版本改了 usage.rs（UsageInfo 加 weekly/monthly 字段 + is_exhausted 三维度）+ lib.rs（删了废弃的 menu_on_left_click）。

**排查方向**（按序）：
1. 直接跑 `cargo run`（debug 模式）看 stdout/stderr 有没有 panic/WebView2 错误（release 的 windows_subsystem 隐藏了控制台，日志看不到）
2. 检查 WebView2 初始化是否失败（首次编译的 exe 能跑，说明 WebView2 本身没问题；怀疑是启动竞态或 panic）
3. 检查 `on_window_event` 的 CloseRequested 逻辑：`window.hide()` + `api.prevent_close()`——如果 setup 阶段窗口还没 show 就触发 hide，可能窗口永远不显示
4. 单实例问题：是否有残留旧进程占着 single-instance 锁导致新进程直接隐藏
5. 临时验证法：把 tauri.conf.json 的窗口 label 确认是 "main"（on_window_event 里 `window.label() == "main"` 匹配）

**接手操作**：修好后 `npx tauri build` 重新打包，启动验证窗口 + 选项卡 + 智能切换按钮，然后跑一次「立即检测并切换」确认三维度判定生效（opencode-3 当前周 71% 或 opencode-1 月 100% 应能触发）。

### 5.2 智能切换三维度判定未实机验证

代码已改（usage.rs：`max_pct = [percent, weekly, monthly] 取最大` 与阈值比较），编译通过，但窗口没起来所以**没在真实环境验证**。接手后验证：当前各 key 用量中 opencode-1 月 100%，点「立即检测并切换」应提示切换或给出耗尽列表。

### 5.3 小瑕疵（不影响功能，可选修）

- env_var / openchatcut / workbuddy 适配器的 `read_key` 不区分 provider，总览页"各软件当前配置"会把同一环境变量的值显示在多个 provider 列下（Python 版同样存在）
- 前端 `SettingsPage` 里配置路径是硬编码显示 `%APPDATA%\KeySwitch\config.toml`（实际一致，但最好由 Rust 端返回）

## 六、测试与验证方法

```bash
# 编译检查
cd /l/00-projects/apikey-switcher-rust/src-tauri && cargo check
# 前端构建
cd /l/00-projects/apikey-switcher-rust && npm run build
# 打包
cd /l/00-projects/apikey-switcher-rust && npx tauri build
# 运行（release exe 位置）
cd /l/00-projects/apikey-switcher-rust/src-tauri/target/release && ./KeySwitch.exe
# 开发模式（热重载）
cd /l/00-projects/apikey-switcher-rust && npm run tauri dev
```

**逻辑测试（无 GUI）**：可直接 `cargo test`（尚无测试用例，建议补 smart_switch_once 的单测，mock query_usage）。
Python 版有完整的逻辑测试参考：`/l/00-projects/apikey-switcher/` 下的测试脚本。

**UI 验证**：computer_use 截图（WebView2 的 UIA 正常，比 tkinter 好）或 PrintWindow 抓窗口。
注意：KeySwitch 窗口可能在副屏（东哥双屏，副屏 DPI 会导致模拟点击坐标偏移，用 UIA element 点击最稳）。

## 七、环境与依赖

- Rust 1.96 / Node 24 / tauri-cli 2.11.1（均已装）
- cargo 走 rsproxy（`~/.cargo/config.toml` 已配），npm 直连
- WebView2：Win11 自带
- 首次 `npx tauri build` 约 5 分钟（依赖编译），增量约 1 分钟

## 八、与 Python 版的关系

- Python 版（`L:\00-projects\apikey-switcher`）**仍在运行可用**（v3.1：主窗口 + 托盘 + 智能切换 + 优先级 + 自助管理），配置在 `config\config.yaml`
- Rust 版是**独立目录的新实现**，配置在 `%APPDATA%\KeySwitch\config.toml`
- 两版 key 池数据目前通过 `tools/switch_opencode_key.py` 保持同步（Rust 版接替后 Python 版可停用）
- 迁移脚本 `tools/migrate_config.py` 可从 Python 版 yaml 重新导入（含 mapping 修复版：内联表写法）

## 九、重要坑（血泪教训）

1. **TOML 数组元素内子映射**：`[targets.0.mapping]` 这种索引式写法在 tomllib/toml crate 里会被解析成 `"0"` 键！**必须用内联表** `mapping = { opencode-go = "opencode-1" }`（migrate_config.py 已修复）
2. **ttk.Label 不支持 fg/bg**（Python 版坑）：Tkinter 的 ttk 部件传 fg/bg 会抛 TclError，Rust 版无此问题
3. **智能切换函数勿自动写盘**（Python 版教训）：测试 mock 配置曾覆盖真实 config.yaml，已从 dist 备份恢复；Rust 版已设计为调用方显式 save
4. **opencode-go 新 key 可能要中国托管 opt-in**：部分新账户访问 deepseek-v4-flash 返回 403 RegionError，需去 `opencode.ai/workspace/<id>/go` 网页同意；当前 sk-xxx 账户实测无需 opt-in（已可用）
5. **副屏 DPI**：KeySwitch 窗口若在副屏，模拟鼠标点击坐标会偏，用 UIA element 点击
6. **usage 三维度**：用户明确要求滚动/周/月**任一**到 100% 都要切换（不要只看滚动值）

## 十、下一步建议（优先级排序）

1. **[紧急] 修复 5.1 窗口不显示问题** → 重新打包 → 验证完整流程（窗口、选项卡、智能切换实测）
2. [建议] 补 Rust 单测：smart_switch_once（mock usage）+ models 的 TOML 解析
3. [可选] 适配器 read_key 按 provider 区分（消除总览页显示瑕疵）
4. [可选] KeySwitch 开机自启（注册表 Run 键 + 启动文件夹，参考 codex-router/hiapi 代理的做法）
5. [可选] 用 `npx tauri build` 产出的安装包做一次干净安装验证（NSIS 安装 → 运行 → 托盘 → 卸载）

---

*生成：2026-08-18 17:40 · Hermes Agent · 本文档同时是 KeySwitch Rust 版唯一权威交接说明，接手人先读第五节再动代码。*
